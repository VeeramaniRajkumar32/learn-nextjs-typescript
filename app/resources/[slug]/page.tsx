'use client'
import { paramsType } from '@/types/project.type'

export default function ResourcePage({ params }: { params: paramsType }) {
    return <div>
       <h1>Resource By Veeramani ❤</h1>
    </div>
}